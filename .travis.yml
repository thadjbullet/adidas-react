language: node_js
node_js:
  - "node"
install:
  - yarn install
before_deploy:
  - yarn global add now
script:
  - yarn lint
deploy:
  provider: script
  on:
    all_branches: true
  skip_cleanup: true
  script:
  - yarn deploy
